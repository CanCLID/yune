---
phase: 05-userdb-and-scaling-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - crates/yune-core/src/lib.rs
  - crates/yune-core/src/engine.rs
  - crates/yune-core/src/key.rs
  - crates/yune-core/src/dictionary/source.rs
  - crates/yune-core/src/translator/mod.rs
  - crates/yune-core/src/filter/mod.rs
  - crates/yune-core/src/tests/engine.rs
  - crates/yune-core/src/tests/translator.rs
  - crates/yune-core/src/tests/filter.rs
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04
must_haves:
  truths:
    - "D-13: Core test cleanup moves tests only by behavior ownership; mechanical moves never mix with semantic behavior changes."
    - "D-14: `crates/yune-core/src/lib.rs` remains a facade; implementation stays in focused modules."
    - "D-15: The summary names owning implementation module, owning test module, and librime comparison target for core behavior coverage."
    - "D-16: Focused core tests and formatting gates run before handoff to final Phase 05 quality closure."
  artifacts:
    - path: "crates/yune-core/src/tests/engine.rs"
      provides: "Behavior-owned engine composition, commit, candidate refresh, and ranking-flow tests split from oversized/facade locations if useful"
    - path: "crates/yune-core/src/tests/translator.rs"
      provides: "Behavior-owned translator and userdb-candidate lookup tests split from oversized locations if useful"
    - path: "crates/yune-core/src/tests/filter.rs"
      provides: "Behavior-owned filter tests split from oversized locations if useful"
  key_links:
    - from: "core oversized or misplaced tests"
      to: "core behavior-owned test modules"
      via: "Move tests without changing assertions or production behavior"
      pattern: "mod tests"
    - from: "crates/yune-core/src/lib.rs"
      to: "focused implementation modules"
      via: "Facade-only module declarations/exports if required"
      pattern: "mod "
---

<objective>
Split remaining useful `yune-core` oversized tests only along behavior ownership boundaries.

Purpose: QUAL-03 requires milestone cleanup without mixing mechanical test movement and semantic userdb behavior. This plan is limited to core-side ownership: engine, translator/userdb-candidate, and filter tests. It preserves D-13 through D-16 by moving assertions without changing runtime semantics and keeping facades thin.

Output: Core behavior-owned test modules where useful, unchanged production behavior except facade/module declarations needed after mechanical moves, and a summary that identifies owned implementation/test/comparison targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-CONTEXT.md
@/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-RESEARCH.md
@/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-PATTERNS.md
@AGENTS.md
@crates/yune-core/src/lib.rs
@crates/yune-core/src/engine.rs
@crates/yune-core/src/translator/mod.rs
@crates/yune-core/src/filter/mod.rs

<interfaces>
Executor must preserve these boundaries:

- This plan is mechanical core test ownership only. It must not change userdb storage semantics from 05-01 or learning/ranking semantics from 05-02.
- D-13 permits splitting tests only by behavior ownership. Acceptable core split targets are engine, translator/userdb candidate behavior, and filter behavior.
- D-14 keeps `lib.rs` and `main.rs` as facades. If moving tests requires module declarations, add only facade-level declarations/exports; do not move implementation into facades.
- D-15 requires the summary to name implementation module, test module, and librime comparison target for each moved or intentionally retained core test group.
- D-16 requires focused tests and formatting before final Phase 05 gates run in 05-04.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory core tests by behavior ownership</name>
  <files>crates/yune-core/src/lib.rs, crates/yune-core/src/engine.rs, crates/yune-core/src/key.rs, crates/yune-core/src/dictionary/source.rs, crates/yune-core/src/translator/mod.rs, crates/yune-core/src/filter/mod.rs, crates/yune-core/src/tests/engine.rs, crates/yune-core/src/tests/translator.rs, crates/yune-core/src/tests/filter.rs</files>
  <read_first>
    - `/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-CONTEXT.md` decisions D-13, D-14, D-15, D-16
    - `/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-PATTERNS.md` core test ownership map and no-facade rule
    - `.planning/codebase/CONCERNS.md` oversized test concerns
    - `.planning/codebase/TESTING.md` focused core test commands
    - `crates/yune-core/src/lib.rs`
  </read_first>
  <action>Identify only `yune-core` tests whose current placement obscures behavior ownership. Use these targets: engine behavior to `crates/yune-core/src/tests/engine.rs`, translator/userdb candidate behavior to `crates/yune-core/src/tests/translator.rs`, and filter behavior to `crates/yune-core/src/tests/filter.rs`. If a candidate test is already behavior-owned or moving it would require semantic changes, leave it in place and record that exact reason for the summary.</action>
  <acceptance_criteria>
    - Candidate inventory is organized by engine, translator/userdb candidate, filter, already behavior-owned, and move-would-require-semantic-change buckets.
    - No API/frontend tests are moved in this plan; those belong to 05-04.
    - No production behavior change is planned from the inventory.
  </acceptance_criteria>
  <verify>
    <automated>grep -R "mod tests" crates/yune-core/src/lib.rs | grep -v '^#' || true</automated>
  </verify>
  <done>Core test movement candidates are identified only by behavior ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Move core tests without changing assertions</name>
  <files>crates/yune-core/src/lib.rs, crates/yune-core/src/engine.rs, crates/yune-core/src/key.rs, crates/yune-core/src/dictionary/source.rs, crates/yune-core/src/translator/mod.rs, crates/yune-core/src/filter/mod.rs, crates/yune-core/src/tests/engine.rs, crates/yune-core/src/tests/translator.rs, crates/yune-core/src/tests/filter.rs</files>
  <read_first>
    - Candidate inventory from Task 1
    - Existing target test modules before edits
  </read_first>
  <action>Move only assertion-preserving core tests into their behavior-owned target modules. Keep production edits limited to module declarations, imports, or test-only visibility required by the mechanical move. Do not alter userdb storage, learning, predictive lookup, backdated scan, candidate ordering semantics, or ABI behavior.</action>
  <acceptance_criteria>
    - `grep -R "mod tests" crates/yune-core/src/lib.rs | grep -v '^#'` shows no large inline behavior test module remains in the core facade unless it is tiny and facade-specific.
    - `grep -R "userdb" crates/yune-core/src/tests/translator.rs` finds any core userdb-candidate translator tests after the split when such tests exist.
    - `git diff -- crates/yune-core/src` shows assertion-preserving test moves and module/import/test-visibility updates only.
    - Any test left unmoved is documented in the summary with the reason `already behavior-owned` or `move would require semantic change`.
  </acceptance_criteria>
  <verify>
    <automated>($HOME/.cargo/bin/cargo test -p yune-core engine -- --nocapture && $HOME/.cargo/bin/cargo test -p yune-core translator -- --nocapture && $HOME/.cargo/bin/cargo test -p yune-core filter -- --nocapture) || (cargo test -p yune-core engine -- --nocapture && cargo test -p yune-core translator -- --nocapture && cargo test -p yune-core filter -- --nocapture)</automated>
  </verify>
  <done>Core behavior-owned test splits are complete without semantic production changes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify core split boundaries and summarize D-15 coverage</name>
  <files>crates/yune-core/src/lib.rs, crates/yune-core/src/tests/engine.rs, crates/yune-core/src/tests/translator.rs, crates/yune-core/src/tests/filter.rs</files>
  <read_first>
    - `.planning/codebase/TESTING.md`
    - `AGENTS.md`
  </read_first>
  <action>Run focused core gates and inspect the diff for mechanical-only changes. The summary must list moved tests, tests intentionally left in place with reason, commands run, and D-15 coverage mapping for core behavior: implementation module, test module, and librime comparison target.</action>
  <acceptance_criteria>
    - `git diff -- crates/yune-core/src` remains dominated by test movement, not broad production rewrites.
    - `grep -R "fn main" crates/yune-core/src/lib.rs | grep -v '^#'` does not show implementation logic in the facade.
    - Focused core test commands pass, or failures are fixed without semantic userdb behavior changes.
  </acceptance_criteria>
  <verify>
    <automated>($HOME/.cargo/bin/cargo fmt --check && $HOME/.cargo/bin/cargo test -p yune-core engine && $HOME/.cargo/bin/cargo test -p yune-core translator && $HOME/.cargo/bin/cargo test -p yune-core filter) || (cargo fmt --check && cargo test -p yune-core engine && cargo test -p yune-core translator && cargo test -p yune-core filter)</automated>
  </verify>
  <done>Core test ownership cleanup passes focused gates and records ownership/comparison coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core test harness to production modules | Test-only visibility/import changes can accidentally expose or alter core production behavior. |
| Core facade to implementation modules | Moving tests can tempt implementation logic into `lib.rs`. |
| Mechanical core test split to semantic behavior | Test movement can accidentally change userdb learning/candidate-order semantics if mixed with behavior edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Core test moves | mitigate | Tasks move tests only by behavior ownership and use diff/focused test gates to ensure assertions are preserved and production semantics do not change. |
| T-05-03-02 | Elevation | `crates/yune-core/src/lib.rs` facade | mitigate | Tasks allow only module declarations/imports, not implementation logic, per D-14. |
| T-05-03-03 | Repudiation | Phase summary ownership evidence | mitigate | Task 3 requires D-15 implementation/test/librime comparison mapping for core coverage. |
| T-05-03-04 | Information Disclosure | Test fixtures | mitigate | This plan does not add real user data fixtures; any fixture movement stays within existing test directories and uses synthetic data. |
</threat_model>

<verification>
Run these gates after task completion. Prefer `$HOME/.cargo/bin/cargo` when `cargo` is not on PATH:

1. `($HOME/.cargo/bin/cargo fmt --check || cargo fmt --check)`
2. `($HOME/.cargo/bin/cargo test -p yune-core engine || cargo test -p yune-core engine)`
3. `($HOME/.cargo/bin/cargo test -p yune-core translator || cargo test -p yune-core translator)`
4. `($HOME/.cargo/bin/cargo test -p yune-core filter || cargo test -p yune-core filter)`

Source audit coverage: GOAL Phase 05 core test ownership cleanup covered; REQ QUAL-03/QUAL-04 covered for core-side gates; RESEARCH test ownership and facade boundaries covered; CONTEXT D-13 through D-16 covered for core test movement.
</verification>

<success_criteria>
- QUAL-03: Remaining useful core tests are split only along behavior ownership boundaries, without semantic behavior changes.
- QUAL-04: Focused core test and formatting gates are executed before final Phase 05 closure.
- D-13 through D-16 are visibly implemented in tasks, acceptance criteria, verification, and summary expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-userdb-and-scaling-hardening/05-03-SUMMARY.md` with core test split inventory, files moved/left in place, commands run, D-15 ownership coverage, and focused gate status.
</output>
