---
phase: 05-userdb-and-scaling-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - crates/yune-rime-api/src/lib.rs
  - crates/yune-rime-api/src/tests/userdb.rs
  - crates/yune-rime-api/src/tests/schema_processors.rs
  - crates/yune-rime-api/src/tests/schema_selection.rs
  - crates/yune-rime-api/tests/frontend_client.rs
  - .planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04
must_haves:
  truths:
    - "D-13: API/frontend test cleanup moves tests only by behavior ownership; mechanical moves never mix with semantic behavior changes."
    - "D-14: `crates/yune-rime-api/src/lib.rs` remains a facade; implementation stays in focused modules."
    - "D-15: Every Phase 05 plan summary names owning implementation module, owning test module, and librime comparison target."
    - "D-16: Phase 05 closes with focused tests, formatting, relevant package tests, workspace tests, and clippy where warranted."
    - "QUAL-03/QUAL-04: Future milestone work can find behavior-owned tests and repeat the exact quality gates."
  artifacts:
    - path: "crates/yune-rime-api/src/tests/userdb.rs"
      provides: "Userdb ABI/session lifecycle tests owned by userdb behavior"
    - path: "crates/yune-rime-api/src/tests/schema_processors.rs"
      provides: "Schema processor tests retained/split only by behavior ownership"
    - path: "crates/yune-rime-api/src/tests/schema_selection.rs"
      provides: "Schema selection/resource behavior tests retained/split only by behavior ownership"
    - path: "crates/yune-rime-api/tests/frontend_client.rs"
      provides: "Frontend-style ABI proof retained as integration ownership, not a dumping ground"
    - path: ".planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md"
      provides: "Codified Phase 05 and future milestone quality gates"
  key_links:
    - from: "API oversized or misplaced tests"
      to: "API/frontend behavior-owned test modules"
      via: "Move tests without changing assertions or production behavior"
      pattern: "mod tests"
    - from: "crates/yune-rime-api/src/lib.rs"
      to: "focused API implementation modules"
      via: "Facade-only module declarations/exports if required"
      pattern: "mod "
    - from: ".planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md"
      to: "Phase 05 summaries"
      via: "Required commands and D-15 ownership checklist"
      pattern: "cargo clippy"
---

<objective>
Split remaining useful `yune-rime-api` and frontend ABI tests by behavior ownership, then codify and run final Phase 05 quality gates.

Purpose: QUAL-03 and QUAL-04 require milestone cleanup and repeatable closure gates without mixing mechanical test movement and semantic userdb behavior. This plan owns API/frontend test placement and final quality-gate codification after 05-01 storage, 05-02 learning, and 05-03 core test cleanup are complete.

Output: API/frontend behavior-owned test modules where useful, unchanged production behavior except facade/module declarations needed after mechanical moves, and a concise Phase 05 quality gate artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/05-userdb-and-scaling-hardening/05-01-SUMMARY.md
@.planning/phases/05-userdb-and-scaling-hardening/05-02-SUMMARY.md
@.planning/phases/05-userdb-and-scaling-hardening/05-03-SUMMARY.md
@/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-CONTEXT.md
@/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-RESEARCH.md
@/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-PATTERNS.md
@AGENTS.md
@crates/yune-rime-api/src/lib.rs
@crates/yune-rime-api/src/tests/userdb.rs
@crates/yune-rime-api/tests/frontend_client.rs

<interfaces>
Executor must preserve these boundaries:

- This plan is mechanical API/frontend test ownership and quality gate codification only. It must not change userdb storage semantics from 05-01, learning/ranking semantics from 05-02, or core test ownership from 05-03 except as required by final gates.
- D-13 permits splitting tests only by behavior ownership. Acceptable API/frontend split targets are userdb, schema processor, schema selection/resource behavior, and frontend-client ABI ownership.
- D-14 keeps `lib.rs` and `main.rs` as facades. If moving tests requires module declarations, add only facade-level declarations/exports; do not move implementation into facades.
- D-15 requires every Phase 05 plan summary to name implementation module, test module, and librime comparison target. This plan codifies that checklist for future execution.
- D-16 requires focused tests, `cargo fmt`, relevant package tests, workspace tests, and clippy where warranted. Prior Phase 04 execution may require `$HOME/.cargo/bin/cargo`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory and split API/frontend tests only along behavior ownership boundaries</name>
  <files>crates/yune-rime-api/src/lib.rs, crates/yune-rime-api/src/tests/userdb.rs, crates/yune-rime-api/src/tests/schema_processors.rs, crates/yune-rime-api/src/tests/schema_selection.rs, crates/yune-rime-api/tests/frontend_client.rs</files>
  <read_first>
    - `/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-CONTEXT.md` decisions D-13, D-14, D-15, D-16
    - `/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-PATTERNS.md` API/frontend test ownership map and no-facade rule
    - `.planning/codebase/CONCERNS.md` oversized test concerns
    - `.planning/codebase/TESTING.md` focused API/frontend test commands
    - `crates/yune-rime-api/src/lib.rs`
  </read_first>
  <action>Identify only API/frontend tests whose current placement obscures behavior ownership and move them to focused test modules without changing test assertions or production semantics. Use these targets: API userdb behavior to `crates/yune-rime-api/src/tests/userdb.rs`, schema processor behavior to `crates/yune-rime-api/src/tests/schema_processors.rs`, schema selection/resource behavior to `crates/yune-rime-api/src/tests/schema_selection.rs`, and frontend ABI behavior to `crates/yune-rime-api/tests/frontend_client.rs`. If a candidate test is already behavior-owned or moving it would require semantic changes, leave it in place and note that in the summary.</action>
  <acceptance_criteria>
    - `grep -R "mod tests" crates/yune-rime-api/src/lib.rs | grep -v '^#'` shows no large inline behavior test module remains in the API facade unless it is tiny and facade-specific.
    - `grep -R "userdb" crates/yune-rime-api/src/tests/userdb.rs` finds API userdb tests in the behavior-owned module after the split.
    - `git diff -- crates/yune-rime-api/src crates/yune-rime-api/tests` shows assertion-preserving test moves and module declarations only; no userdb storage, learning, ranking, or ABI semantic changes are introduced by this plan.
    - Any test left unmoved is documented in the summary with the reason `already behavior-owned` or `move would require semantic change`.
  </acceptance_criteria>
  <verify>
    <automated>($HOME/.cargo/bin/cargo test -p yune-rime-api schema_selection -- --nocapture && $HOME/.cargo/bin/cargo test -p yune-rime-api userdb -- --nocapture && $HOME/.cargo/bin/cargo test -p yune-rime-api --test frontend_client -- --nocapture) || (cargo test -p yune-rime-api schema_selection -- --nocapture && cargo test -p yune-rime-api userdb -- --nocapture && cargo test -p yune-rime-api --test frontend_client -- --nocapture)</automated>
  </verify>
  <done>Remaining useful API/frontend test splits are complete by behavior ownership only; production behavior is unchanged; facades remain facades.</done>
</task>

<task type="auto">
  <name>Task 2: Codify Phase 05 quality gates and ownership checklist</name>
  <files>.planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md</files>
  <read_first>
    - `/Users/trenton/Projects/yune/.planning/phases/05-userdb-and-scaling-hardening/05-CONTEXT.md` decisions D-15, D-16
    - `.planning/codebase/TESTING.md`
    - `.planning/phases/05-userdb-and-scaling-hardening/05-01-SUMMARY.md`
    - `.planning/phases/05-userdb-and-scaling-hardening/05-02-SUMMARY.md`
    - `.planning/phases/05-userdb-and-scaling-hardening/05-03-SUMMARY.md`
  </read_first>
  <action>Create `05-QUALITY-GATES.md` for Phase 05 execution closure. Include: (1) focused command groups for storage lifecycle, learning/predictive flow, core test splits, API/frontend test splits, and frontend ABI proof; (2) the fallback cargo invocation rule using `$HOME/.cargo/bin/cargo` before `cargo` if PATH is incomplete; (3) the D-15 checklist requiring implementation module, test module, and librime comparison target in every summary; (4) the D-16 final gate commands: `cargo fmt --check`, relevant `cargo test -p ...`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets -- -D warnings`; (5) a rule that future test splits must be separate from semantic behavior changes. Do not create broad process documentation outside the phase directory; this artifact is a phase execution gate, not general docs.</action>
  <acceptance_criteria>
    - `grep -R "D-15" .planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md` finds the ownership checklist.
    - `grep -R "D-16" .planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md` finds final gate commands.
    - `grep -R "cargo clippy --workspace --all-targets -- -D warnings" .planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md` finds the clippy gate.
    - `grep -R "semantic behavior changes" .planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md` finds the rule separating mechanical test splits from behavior changes.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md && grep -q "D-15" .planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md && grep -q "D-16" .planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md</automated>
  </verify>
  <done>Phase 05 has a local, executable quality gate artifact with ownership/comparison checklist and exact cargo commands.</done>
</task>

<task type="auto">
  <name>Task 3: Run final milestone-quality verification without semantic changes</name>
  <files>crates/yune-rime-api/src/lib.rs, crates/yune-rime-api/src/tests/userdb.rs, crates/yune-rime-api/src/tests/schema_processors.rs, crates/yune-rime-api/src/tests/schema_selection.rs, crates/yune-rime-api/tests/frontend_client.rs, .planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md</files>
  <read_first>
    - `.planning/phases/05-userdb-and-scaling-hardening/05-QUALITY-GATES.md`
    - `.planning/codebase/TESTING.md`
    - `AGENTS.md`
  </read_first>
  <action>Run the final Phase 05 quality gates from `05-QUALITY-GATES.md`. Fix only mechanical issues caused by test moves, such as imports, module declarations, visibility for tests, or formatting. Do not change userdb storage, sync, recovery, learning, predictive lookup, backdated scan, candidate ordering, or ABI semantics in this plan. Ensure the final summary lists any tests not split, why they remained in place, commands run, and D-15 owner/test/comparison target coverage for all four Phase 05 plans.</action>
  <acceptance_criteria>
    - `git diff --stat` shows this plan is dominated by API/frontend test-module movement and `05-QUALITY-GATES.md`, not broad production rewrites.
    - `grep -R "fn main" crates/yune-cli/src/main.rs crates/yune-rime-api/src/lib.rs crates/yune-core/src/lib.rs | grep -v '^#'` does not show new implementation logic in facades.
    - All command outputs in `<verify>` pass, or any failure is fixed without semantic userdb behavior changes.
  </acceptance_criteria>
  <verify>
    <automated>($HOME/.cargo/bin/cargo fmt --check && $HOME/.cargo/bin/cargo test -p yune-core engine && $HOME/.cargo/bin/cargo test -p yune-core translator && $HOME/.cargo/bin/cargo test -p yune-core filter && $HOME/.cargo/bin/cargo test -p yune-rime-api userdb && $HOME/.cargo/bin/cargo test -p yune-rime-api schema_selection && $HOME/.cargo/bin/cargo test -p yune-rime-api --test frontend_client && $HOME/.cargo/bin/cargo test --workspace && $HOME/.cargo/bin/cargo clippy --workspace --all-targets -- -D warnings) || (cargo fmt --check && cargo test -p yune-core engine && cargo test -p yune-core translator && cargo test -p yune-core filter && cargo test -p yune-rime-api userdb && cargo test -p yune-rime-api schema_selection && cargo test -p yune-rime-api --test frontend_client && cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings)</automated>
  </verify>
  <done>Final Phase 05 gates pass after behavior-owned test splits; no semantic implementation changes are introduced by this quality plan; future execution has codified gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/frontend test harness to production modules | Test-only visibility/import changes can accidentally expose or alter ABI behavior. |
| API facade to implementation modules | Moving tests can tempt implementation logic into `lib.rs`. |
| Quality gate artifact to future execution | Incorrect or incomplete commands can create false confidence for future milestone closure. |
| Mechanical API/frontend test split to semantic behavior | Test movement can accidentally change userdb/storage/learning semantics if mixed with behavior edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | API/frontend test moves | mitigate | Task 1 moves tests only by behavior ownership and uses diff/command gates to ensure assertions are preserved and production semantics do not change. |
| T-05-04-02 | Elevation | `crates/yune-rime-api/src/lib.rs` facade | mitigate | Tasks 1 and 3 grep facade files and allow only module declarations/imports, not implementation logic, per D-14. |
| T-05-04-03 | Repudiation | Phase summary ownership evidence | mitigate | Task 2 codifies D-15 checklist so summaries identify implementation owner, test owner, and librime comparison target. |
| T-05-04-04 | Denial of Service | Overbroad workspace quality gates | accept | Workspace/clippy gates can be slower but are required by D-16 for final closure; no runtime DoS surface is added. |
| T-05-04-05 | Information Disclosure | Test fixtures and snapshots | mitigate | This plan does not add real user data fixtures; any fixture movement stays within existing test directories and uses synthetic data. |
</threat_model>

<verification>
Run these gates after task completion. Prefer `$HOME/.cargo/bin/cargo` when `cargo` is not on PATH:

1. `($HOME/.cargo/bin/cargo fmt --check || cargo fmt --check)`
2. `($HOME/.cargo/bin/cargo test -p yune-core engine || cargo test -p yune-core engine)`
3. `($HOME/.cargo/bin/cargo test -p yune-core translator || cargo test -p yune-core translator)`
4. `($HOME/.cargo/bin/cargo test -p yune-core filter || cargo test -p yune-core filter)`
5. `($HOME/.cargo/bin/cargo test -p yune-rime-api userdb || cargo test -p yune-rime-api userdb)`
6. `($HOME/.cargo/bin/cargo test -p yune-rime-api schema_selection || cargo test -p yune-rime-api schema_selection)`
7. `($HOME/.cargo/bin/cargo test -p yune-rime-api --test frontend_client || cargo test -p yune-rime-api --test frontend_client)`
8. `($HOME/.cargo/bin/cargo test --workspace || cargo test --workspace)`
9. `($HOME/.cargo/bin/cargo clippy --workspace --all-targets -- -D warnings || cargo clippy --workspace --all-targets -- -D warnings)`

Source audit coverage: GOAL Phase 05 remaining quality/scaling concerns covered; REQ QUAL-03/QUAL-04 covered; RESEARCH test ownership, facade boundaries, cargo fallback, focused/workspace/clippy gates covered; CONTEXT D-13 through D-16 covered.
</verification>

<success_criteria>
- QUAL-03: Remaining useful API/frontend compatibility tests are split only along behavior ownership boundaries, without semantic behavior changes.
- QUAL-04: Phase 05 quality gates are codified and executed with focused tests, formatting, relevant package tests, workspace tests, and clippy.
- D-13 through D-16 are visibly implemented in tasks, acceptance criteria, verification, and summary expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-userdb-and-scaling-hardening/05-04-SUMMARY.md` with API/frontend test split inventory, files moved/left in place, commands run, D-15 ownership coverage for all four Phase 05 plans, and final Phase 05 gate status.
</output>
