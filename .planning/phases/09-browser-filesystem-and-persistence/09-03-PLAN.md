---
phase: 09-browser-filesystem-and-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - packages/yune-typeduck-runtime/src/filesystem.ts
  - packages/yune-typeduck-runtime/test/filesystem.test.ts
  - docs/typeduck-web-adapter.md
autonomous: true
requirements:
  - TYPEDUCK-FS-04
must_haves:
  truths:
    - "Browser host callers can see deterministic setup failures for missing assets and invalid preload state."
    - "Browser host callers can see deterministic sync failures and know whether persisted, stale in-memory, or unpersisted state is involved."
    - "Browser host callers have documented local-first recovery order for missing assets, failed sync, and stale deployed configs."
    - "Phase 09 remains limited to package-local helpers and docs, not TypeDuck-Web app integration, network fetch policy, browser E2E, native export expansion, or AI behavior."
  artifacts:
    - path: "packages/yune-typeduck-runtime/test/filesystem.test.ts"
      provides: "Focused failure-mode tests for missing assets, failed sync, and recovery-relevant ordering"
    - path: "packages/yune-typeduck-runtime/src/filesystem.ts"
      provides: "Stable failure messages and optional recovery-order helper comments/types if needed by tests/docs"
    - path: "docs/typeduck-web-adapter.md"
      provides: "Phase 09 browser filesystem helper usage, IDBFS/equivalent sync policy, userdb sync boundaries, and recovery instructions"
  key_links:
    - from: "docs/typeduck-web-adapter.md"
      to: "packages/yune-typeduck-runtime/src/filesystem.ts"
      via: "Documented helper imports and flow"
      pattern: "prepareTypeDuckFilesystem|syncFromPersistenceBeforeInit|deployAndSync|customizeAndSync"
    - from: "docs/typeduck-web-adapter.md"
      to: "Phase 10 boundary"
      via: "Current scope/non-goals"
      pattern: "Phase 10"
---

<objective>
Close Phase 09 by making failure and recovery behavior visible in tests and documentation.

Purpose: The filesystem helpers are only browser-usable if callers can recover deterministically from missing assets, failed persistence sync, and stale deployed config state without hidden network/app policy. This plan implements D-12 through D-14 and TYPEDUCK-FS-04.
Output: Focused failure-mode tests and updated TypeDuck-Web adapter documentation covering helper usage, IDBFS/equivalent sync timing, userdb sync boundaries, stale deployed config recovery, and out-of-scope Phase 10 items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trenton/Projects/yune/.planning/PROJECT.md
@/Users/trenton/Projects/yune/.planning/ROADMAP.md
@/Users/trenton/Projects/yune/.planning/STATE.md
@/Users/trenton/Projects/yune/.planning/REQUIREMENTS.md
@/Users/trenton/Projects/yune/.planning/phases/09-browser-filesystem-and-persistence/09-CONTEXT.md
@/Users/trenton/Projects/yune/.planning/phases/09-browser-filesystem-and-persistence/09-RESEARCH.md
@/Users/trenton/Projects/yune/.planning/phases/09-browser-filesystem-and-persistence/09-PATTERNS.md
@/Users/trenton/Projects/yune/.planning/phases/09-browser-filesystem-and-persistence/09-01-SUMMARY.md
@/Users/trenton/Projects/yune/.planning/phases/09-browser-filesystem-and-persistence/09-02-SUMMARY.md
@/Users/trenton/Projects/yune/packages/yune-typeduck-runtime/src/filesystem.ts
@/Users/trenton/Projects/yune/packages/yune-typeduck-runtime/test/filesystem.test.ts
@/Users/trenton/Projects/yune/docs/typeduck-web-adapter.md
@/Users/trenton/Projects/yune/scripts/typeduck-exports.txt

<interfaces>
Document the helpers produced by Plans 09-01 and 09-02. Expected public names:
```typescript
prepareTypeDuckFilesystem
assertTypeDuckAssetsReady
syncFromPersistenceBeforeInit
syncToPersistenceAfterMutation
syncAfterUserDataChange
deployAndSync
customizeAndSync
TypeDuckFilesystemError
```

Recovery order to preserve per D-13/D-14:
```text
1. sync from persistence with syncfs(true)
2. ensure shared/user/build layout
3. preload explicit default/schema/dictionary assets
4. verify required shared and build files
5. deploy/customize with a live runtime only when required by the recovery case
6. sync to persistence with syncfs(false)
7. initialize or reinitialize only after required files are complete
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failure-mode tests for missing assets, failed sync, and recovery ordering</name>
  <files>packages/yune-typeduck-runtime/src/filesystem.ts, packages/yune-typeduck-runtime/test/filesystem.test.ts</files>
  <behavior>
    - Missing shared `default.yaml`, missing shared schema, missing dictionary, missing build `default.yaml`, and missing build schema each produce `TypeDuckFilesystemError` with the missing virtual path per D-12.
    - A failed before-init `syncfs(true)` rejects before any runtime init attempt can occur, preserving visible failure per D-10/D-12.
    - A failed after-deploy/customize `syncfs(false)` rejects after the runtime mutation and records that in-memory changes may be unpersisted per D-10/D-12.
    - Stale deployed config recovery is represented by a deterministic tested sequence where preload/verify/deploy/sync ordering is explicit; if no production helper is needed, keep this as a test fixture asserting call order plus docs in Task 2 per D-13.
  </behavior>
  <action>Strengthen `filesystem.test.ts` to cover the concrete failure matrix from `09-RESEARCH.md`: missing shared default, missing shared schema, wrong/missing dictionary path, missing build default/schema, invalid logical IDs, failed sync before init, failed sync after deploy/customize, and stale deployed config recovery ordering. Adjust `src/filesystem.ts` only if needed to make error messages stable and actionable; do not add a YAML parser or network fetch. For stale config, prefer a test that proves the deterministic local-first order available from existing helpers rather than adding metadata heuristics the helper cannot know. Ensure tests do not depend on real Emscripten, IndexedDB, DOM, timers, or TypeDuck-Web source per D-02/D-14.</action>
  <verify>
    <automated>npm --prefix /Users/trenton/Projects/yune/packages/yune-typeduck-runtime test -- filesystem.test.ts</automated>
  </verify>
  <done>Focused Vitest coverage demonstrates visible failures and deterministic recovery-relevant ordering for all Phase 09 failure modes that can be tested without real browser E2E.</done>
</task>

<task type="auto">
  <name>Task 2: Update browser filesystem and recovery documentation</name>
  <files>docs/typeduck-web-adapter.md</files>
  <action>Update `docs/typeduck-web-adapter.md` to replace Phase 9 deferral wording with the implemented TypeScript helper contract per D-01/D-02. Document: package-local import example; caller-provided `sharedDataDir`, `userDataDir`, `schemaId`, and `dictionaryId` per D-03; required layout and assets per D-04/D-05; logical ID rule and no placeholder behavior per D-06/D-07; IDBFS or equivalent mount/sync policy with `syncfs(true)` before init and `syncfs(false)` after deploy/customize/userdb-changing host boundaries per D-08/D-09; deterministic `TypeDuckFilesystemError` sync/setup failures per D-10/D-12; convenience wrappers around `runtime.deploy()` and `runtime.customize(...)` without changing `TypeDuckRuntime` lifecycle per D-11; stale deployed config recovery order that reruns explicit preload/deploy/sync flows before init where possible per D-13; and local-first recovery with no network fetch, remote discovery, or TypeDuck-Web app policy per D-14. Keep Phase 10 items explicitly out of scope: upstream TypeDuck-Web clone/patch/E2E, browser app policy, network/CDN/cache/service worker, native export expansion, multi-instance isolation, and AI behavior.</action>
  <verify>
    <automated>npm --prefix /Users/trenton/Projects/yune/packages/yune-typeduck-runtime test -- filesystem.test.ts && npm --prefix /Users/trenton/Projects/yune/packages/yune-typeduck-runtime run build</automated>
  </verify>
  <done>Docs show the helper-supported browser filesystem flow, sync timing, recovery instructions, and scoped non-goals without promising app-specific/browser-E2E behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 09 scope and source gates</name>
  <files>packages/yune-typeduck-runtime/src/filesystem.ts, packages/yune-typeduck-runtime/test/filesystem.test.ts, docs/typeduck-web-adapter.md</files>
  <action>Run the final package gates and grep gates to prove Phase 09 stayed within scope. If any gate fails, fix the implementation/docs rather than weakening the gate. Do not run Rust native tests unless this plan unexpectedly modified Rust adapter files or `scripts/typeduck-exports.txt`; Phase 09 should not require native export changes. Ensure docs and tests mention userdb persistence as explicit host sync boundaries rather than automatic notification coverage, because current exports do not expose mutation notifications.</action>
  <verify>
    <automated>npm --prefix /Users/trenton/Projects/yune/packages/yune-typeduck-runtime test && npm --prefix /Users/trenton/Projects/yune/packages/yune-typeduck-runtime run build && if grep -R "fetch\|serviceWorker\|upstream TypeDuck-Web checkout\|Playwright\|Cypress\|AI-native" /Users/trenton/Projects/yune/packages/yune-typeduck-runtime/src /Users/trenton/Projects/yune/packages/yune-typeduck-runtime/test /Users/trenton/Projects/yune/docs/typeduck-web-adapter.md | grep -v '^#'; then exit 1; fi && if grep -R "yune_typeduck_.*sync\|yune_typeduck_.*userdb" /Users/trenton/Projects/yune/scripts/typeduck-exports.txt /Users/trenton/Projects/yune/packages/yune-typeduck-runtime/src | grep -v '^#'; then exit 1; fi</automated>
  </verify>
  <done>All package tests/build pass, docs/source grep gates show no deferred Phase 10/network/AI/native-export scope, and Phase 09 failure/recovery behavior is covered by tests/docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Missing/stale browser assets -> helper recovery | Caller receives missing or stale virtual filesystem state that must fail visibly. |
| Emscripten sync callback -> documented caller state | Sync callback errors determine whether caller has persisted, stale, or unpersisted browser state. |
| Phase 09 helper/docs -> Phase 10 app integration | Documentation must not accidentally encode TypeDuck-Web app policy or deferred network/browser E2E behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | Missing asset recovery | mitigate | Tests and docs require visible `TypeDuckFilesystemError` with actionable missing paths, not best-effort continuation per D-12. |
| T-09-10 | Repudiation | Failed sync recovery | mitigate | Docs identify whether before-init or after-mutation sync failed and what state may be stale/unpersisted per D-10/D-12. |
| T-09-11 | Tampering | Stale deployed config | mitigate | Document deterministic local-first recovery order: sync, preload, verify, deploy/customize if needed, sync, then init per D-13/D-14. |
| T-09-12 | Elevation of Privilege | Deferred app/network/native behavior | mitigate | Grep gates and docs keep network fetch, service workers, TypeDuck-Web app patching, native export expansion, and AI behavior out of Phase 09 per deferred decisions. |
</threat_model>

<verification>
Run final package gates:

```bash
npm --prefix /Users/trenton/Projects/yune/packages/yune-typeduck-runtime test
npm --prefix /Users/trenton/Projects/yune/packages/yune-typeduck-runtime run build
```

Run scope gates:

```bash
if grep -R "fetch\|serviceWorker\|upstream TypeDuck-Web checkout\|Playwright\|Cypress\|AI-native" /Users/trenton/Projects/yune/packages/yune-typeduck-runtime/src /Users/trenton/Projects/yune/packages/yune-typeduck-runtime/test /Users/trenton/Projects/yune/docs/typeduck-web-adapter.md | grep -v '^#'; then exit 1; fi
if grep -R "yune_typeduck_.*sync\|yune_typeduck_.*userdb" /Users/trenton/Projects/yune/scripts/typeduck-exports.txt /Users/trenton/Projects/yune/packages/yune-typeduck-runtime/src | grep -v '^#'; then exit 1; fi
```
</verification>

<success_criteria>
- Missing asset and sync failure behavior is covered by deterministic tests or documented repro/recovery instructions.
- Stale deployed config recovery order is explicit, local-first, and does not initialize with incomplete state.
- Documentation now presents Phase 09 helper-supported filesystem/persistence flow while keeping Phase 10 and AI-native items out of scope.
- Full package tests and build pass.
</success_criteria>

<output>
After completion, create `/Users/trenton/Projects/yune/.planning/phases/09-browser-filesystem-and-persistence/09-03-SUMMARY.md`
</output>
