# Rime schema
# encoding: utf-8

__include: template:/

schema:
  schema_id: jyut6ping3_mobile
  name: 粵語拼音
  dependencies:
    - luna_pinyin

engine:
  processors:
    - ascii_composer
    - recognizer
    - key_binder
    - speller
    - punctuator
    - selector
    - navigator
    - express_editor
  segmentors:
    - ascii_segmentor
    - matcher
    - affix_segmentor@luna_pinyin
    - abc_segmentor
    - punct_segmentor
    - fallback_segmentor
  translators:
    - punct_translator
    - script_translator
    - script_translator@luna_pinyin
  filters:
    - dictionary_lookup_filter
    - simplifier@output_hk2s
    - simplifier@output_t2s
    - simplifier@output_s2t
    - simplifier@output_t2tw
    - simplifier@output_s2tw
    - simplifier@output_tw2s
    - simplifier@output_tw2t

menu:
  __patch: include:/no_candidate_selection

translator:
  prism: jyut6ping3_mobile
  __patch: include:/letter_to_tone
  prediction_candidate_limit: 1
  prefix_fallback: true

output_hk2s:
  option_name: output_hk2s
  opencc_config: hk2s.json

output_t2s:
  option_name: output_t2s
  opencc_config: t2s.json

output_s2t:
  option_name: output_s2t
  opencc_config: s2t.json

output_t2tw:
  option_name: output_t2tw
  opencc_config: t2tw.json

output_s2tw:
  option_name: output_s2tw
  opencc_config: s2tw.json

output_tw2s:
  option_name: output_tw2s
  opencc_config: tw2s.json

output_tw2t:
  option_name: output_tw2t
  opencc_config: tw2t.json

luna_pinyin:
  tag: luna_pinyin
  dictionary: luna_pinyin_yune_reverse
  enable_sentence: false
  enable_user_dict: false
  encode_commit_history: false
  always_show_comments: true
  prefix: "`"
  suffix: ";"
  tips: 〔普通話〕
  preedit_format:
    - xform/([nl])v/$1ü/
    - xform/([nl])ue/$1üe/
    - xform/([jqxy])v/$1u/
  comment_format:
    - xform/^/\v/

recognizer:
  import_preset: default
  patterns:
    luna_pinyin: "^`[a-z']*;?$"

__patch:
  - common:/disable_completion?
  - common:/enable_correction?
  - common:/disable_sentence?
  - common:/disable_learning?
  - common:/separate_candidates?
