# M55 Final Closeout

Date: 2026-07-04

Verdict: **green / complete for Tier M bounded native Track A performance**.

M55 is closed by the two consecutive final default-on runs:

- `default-on-ratchet-5-config-cache/`
- `default-on-ratchet-6-config-cache/`

Both runs used the landed product path with no `YUNE_POET_BYTE_BACKED` opt-in.
The runtime consumes validated `YUNE-POET/2` byte-backed Luna poet artifacts by
default; `YUNE_POET_BYTE_BACKED=0` remains an explicit diagnostic opt-out.

## Final Command

```powershell
Remove-Item Env:YUNE_POET_BYTE_BACKED -ErrorAction SilentlyContinue
powershell -ExecutionPolicy Bypass -File scripts\benchmark-native-rime-inprocess.ps1 `
  -OutputRoot docs\reports\evidence\m55-native-match-or-beat\phase-5-final\default-on-ratchet-6-config-cache `
  -Iterations 9 -SessionIterations 60 -KeyIterations 80 `
  -TrackAInputs n,ni,hao,zhongguo,ceshiyixiachangjushuruxingnengzenyang,zhegeyinqingqishiyinggaizhichichaochangjuzishurucainengyong,cszysmsrsd,zybfshmsru `
  -TrackBInputs neigojangingkeisatjinggoiziwunciucoenggeoizisyujapsinhojijung `
  -DeployProductBeforeBenchmark `
  -TrackAThresholds docs\reports\evidence\m55-native-match-or-beat\thresholds\m55-thresholds.csv `
  -FailOnRegression
```

The same command shape produced the companion green run under
`default-on-ratchet-5-config-cache/`.

## Final Run 6 Summary

| Row | Observed | Ceiling | Status |
| --- | ---: | ---: | --- |
| startup ratio | `0.286x` | `1.101x` | pass |
| session median | `6,679.500 us` | `25,533.310 us` | pass |
| `n` ratio | `1.794x` | `2.000x` | pass |
| `ni` ratio | `1.039x` | `2.000x` | pass |
| `hao` ratio | `0.815x` | `1.750x` | pass |
| 37-char ratio | `0.237x` | `1.500x` | pass |
| 59-char ratio | `0.086x` | `1.500x` | pass |
| `zhongguo` ratio | `0.038x` | `0.325x` | pass |
| `cszysmsrsd` ratio | `0.090x` | `0.532x` | pass |
| `zybfshmsru` ratio | `0.123x` | `0.770x` | pass |
| Track A peak working set | `113,397,760 B` | `118,831,104 B` | pass |
| Track B long-row latency | `316.282 us` | `375.253 us` | pass |

Final run 5 also passed the same threshold file. Its Track A peak was
`113,270,784 B`; final run 6 was the higher of the two at `113,397,760 B`.

## Landing Notes

- Byte-backed poet consumption is default-on only after two flag-on full M55
  ratchets were green:
  `phase-3r-short-key-alias-byte-backed-full-ratchet/` and
  `phase-3r-short-key-alias-byte-backed-full-ratchet-2/`.
- The tightened threshold artifact is
  `docs/reports/evidence/m55-native-match-or-beat/thresholds/m55-thresholds.csv`.
  It caps `n`/`ni` at `<=2.00x`, `hao` at `<=1.75x`, both long Luna rows at
  `<=1.50x`, and Track A peak at `118,831,104 B`.
- `default-on-ratchet-2/` and `default-on-ratchet-4/` failed only the inherited
  startup ratio row before the runtime config cache landed. They remain in the
  evidence tree as rejected closeout attempts.
- The final startup/session fix is a metadata-validated runtime config cache for
  parsed deployed YAML. It removes repeated parsing in schema application while
  preserving reload behavior when the file path metadata changes.
- Candidate snapshot evidence in the final benchmark runs shows the long-row
  Yune candidates remain non-empty and consistent with the accepted M55
  product-path candidate fixture. M55 does not claim that every benchmark row
  ranks identically to librime.

## Scope Boundaries

- No public C ABI field order changed.
- No browser, web-runtime, frontend, product, platform, AI, or plugin ABI scope
  was added.
- TypeDuck `jyut6ping3_mobile` remained a Track B regression guard, not a
  same-run librime comparison lane.
- Full third-party `.gram` bytes remain untracked.
