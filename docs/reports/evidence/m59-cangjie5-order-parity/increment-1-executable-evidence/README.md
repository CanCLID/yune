# M59 Increment 1 - Cangjie executable pre-fix diagnostic

Generated on 2026-07-10 from clean Yune commit
`c7c04ff73b76ea3e8e1c5e6bf9b432483ee6650f` in the ignored scratch root
`target/m59-increment1-cangjie-final/`, then copied byte-identically into this
seven-file packet.

**Verdict: RED, 4/12 accepted and 8/12 failed.** This is executable pre-fix
diagnostic evidence for M59/D-48 Cangjie finding CJ-1. It is not acceptance
evidence, does not close D-48 or M59, removes no ignored blocker, and applies
no exceptions. The exact comparator exited `1`, as required for the preserved
red rows.

## Generation and import identity

| Generated and imported file | Bytes | SHA-256 |
|---|---:|---|
| `cangjie-oracle-raw.json` | 162,125 | `91dca789769cbbed160132bf23a54d891bf164f3f2617d5fcf5a2ac5d4443be1` |
| `cangjie-oracle.json` | 66,584 | `24408c3b2b83db516ae1382d2ba743b41ead50c7c026aee2837a01137c7ecbcf` |
| `cangjie-yune.json` | 119,312 | `82fe38e0ef24bea76b3e65400cb9605fb2579ad9ea7d8caf5ca8c5bfbe216971` |
| `cangjie-diff.json` | 19,310 | `c25d427e3d229d0801b38142551b5c22707365b1d2b50be5c47acac41f15fe89` |
| `cangjie-diff.csv` | 7,590 | `281350505562b093c5d72b7bc86c116660ec570d277b5ddc3db7c4edf2879349` |

All five packet copies retain the generated bytes exactly. Their embedded
scratch paths were not rewritten. The raw oracle, curated oracle, and Yune
capture were create-new outputs. The oracle and Yune captures were each
repeated at the same logical output paths after removing the prior outputs;
the repeat bytes and SHA-256 values were identical to the final files above.
Both final capture commands exited `0`.

The comparator inputs were then frozen, the first JSON/CSV outputs were
preserved as ignored `cangjie-diff-first.*` snapshots, and the comparator was
rerun to the same canonical output paths. It again exited `1` and reproduced
both hashes above byte-for-byte.

The preserved diagnostic file
`target/m59-increment1-cangjie-final/cangjie-yune-timeout-preserved.json`
remains ignored under `target/` and is deliberately not imported. It happens
to match the successful final Yune bytes, but it is not part of the canonical
generation chain or this packet.

All seven packet files are canonical UTF-8 without BOM, use LF only, contain
no NUL or CR byte, and end in exactly one LF. Repository attributes resolve
these paths to `text=auto eol=lf`; filtered and no-filter Git blob IDs must be
identical after staging.

## Oracle identity and hardened target provenance

- Engine: `rime/librime` 1.17.0 at
  `33e78140250125871856cdc5b42ddc6a5fcd3cd4`.
- Schema: upstream `rime/rime-cangjie` at
  `52d90a1b1312e74042b38c1cbc8142defbc53171`, Git tree
  `db11cf6ffd382ada3087e9765c0ba2e636a8b68d`.
- All seven schema/dependency repositories are pinned by commit and Git tree
  and were clean before and after capture.
- `rime.dll`: SHA-256
  `86b4c7357d4c6d293ce5589b234d8859ca2ac30923a03bedfa3926eeaf97fb0b`.
- `rime_deployer.exe`: SHA-256
  `3abb72b5bb56fcafcfe925d533ae5f832c68d5a0bc9952fd0eea0682fb1ab071`.
- Capture wrapper: `scripts/capture-upstream-cangjie5.ps1`, SHA-256
  `bd70748a7f95434dba3739d3c13c9d8f468d053aef8b350b89248ce3aa22012c`.
- Curator: `scripts/curate-upstream-cangjie5.py` v2, SHA-256
  `2e69df2649f514b090897ffbb1624c547caed7e81bd05b4d6b0702f584b1e3d1`.
- Shared probe: `scripts/oracle-rime-probe.cs`, SHA-256
  `94f7deb7c3632a6c3c918536295b03d88aa8a80bbbbc9d8a26e896fb70bf07e7`.
- Source/shared, staged/shared, and deployed/build tree hashes are respectively
  `0d614d5ebe43e22f04cf172d4ce85f1a771b5fee8512378b71461ae658c5d5e3`,
  `bb6731efa39c4ed0a2d8c08a6e4359a239bb6e849daac196e351a04f2d52b8c3`,
  and `509ba22d6999b8818535ace10a1c05379c2d65d73e997db716e19f7587077556`.

The raw capture contains the ordered 12-input fixture and the shared ordered
base option policy:
`ascii_mode=false`, `full_shape=false`, `ascii_punct=false`, and
`zh_hans=false`. There are no additional runtime option patches. Every case
has page size 5, complete all-page capture, a present menu, and
`termination_reason=last_page`.

The curated oracle derives the three owner targets from each whole-input
`all_candidates[0].text`, verifies them against the owner-declared ASCII U+
specifications, and reconstructs each from the corresponding atomic
candidate-zero cases:

| Input | Derived oracle candidate 0 | Verified codepoints |
|---|---|---|
| `hwmvsqtt` | 粵拼 | `U+7CB5 U+62FC` |
| `ebcnyripm` | 測試 | `U+6E2C U+8A66` |
| `takohaeosk` | 莫伯洢 | `U+83AB U+4F2F U+6D22` |

The controls are likewise linked to captured candidates: `hdaetcu` gives
香港 at position 0 and `lyk` gives 中文 at position 1.

## Tracked fixture correction

The curated oracle was imported byte-identically as
`crates/yune-core/tests/fixtures/upstream-1.17.0/cangjie5-composition.json`.
The previous fixture had SHA-256
`b694f9720a96d7473e1e5d0621785562ae12fab247dc86bd0f22a435924b3e52`;
the corrected fixture has SHA-256
`24408c3b2b83db516ae1382d2ba743b41ead50c7c026aee2837a01137c7ecbcf`.

For all 12 inputs, the ordered candidate arrays, page arrays,
selected-candidate arrays, commit previews, and captured-all-pages results are
unchanged. The fixture correction:

- replaces three mojibaked, hand-embedded `composition_rows.target` strings
  and stale `oracle_backed pending capture` wording with targets derived from
  the captured candidate bytes;
- records the untouched raw-capture hash and exact curator policy;
- adds clean source commit/tree, tool, binary, command, option, paging, and
  output-provenance identities;
- makes `rime_get_input` the exact requested logical input rather than a
  formatted/segmented display string;
- adds explicit `num_candidates`, `candidate_pointer_null`, `menu_present`,
  and `termination_reason` fields; and
- adds the source-schema Git tree while preserving the same source cohorts and
  vocabulary rows.

## Yune identity and staging

- Clean source commit:
  `c7c04ff73b76ea3e8e1c5e6bf9b432483ee6650f`, Git tree
  `382f7d31617a5a6aeb2cd9e1ec7472b7bf3e3bc1`, empty status.
- Release DLL: SHA-256
  `2ac5d2550cd462d52d11aec995f609a605ccc6aa9837a8e55cec18a7ec63cfa5`.
- Capture script: `scripts/capture-yune-candidate-order.ps1`, SHA-256
  `c2614bc7f068d89903d7b0af3856f286f07194e35f505806251aa2ba887aa45c`.
- Shared probe: SHA-256
  `94f7deb7c3632a6c3c918536295b03d88aa8a80bbbbc9d8a26e896fb70bf07e7`.
- Source and staged shared-data tree hashes are
  `bb6731efa39c4ed0a2d8c08a6e4359a239bb6e849daac196e351a04f2d52b8c3`
  and `b88935676beb1c5430de142117f3dcbb7ce20d3a2e79089f54bd7851442e2b3b`.
- Schema narrowing was explicit through `-NarrowSchemaList`; no tracked
  `default.yaml` overlay was used.

Yune derived the exact ordered input list from the curated oracle. Every Yune
case also records complete all-page capture, a present menu, and last-page
termination, so the red verdicts are behavior differences rather than capture
truncation.

## Exact verdicts

`First mismatch` is zero-based. No exception file was supplied.

| Input | Oracle | Yune | First mismatch | Missing | Extra | Failure classes | Verdict |
|---|---:|---:|---:|---:|---:|---|---|
| `hwmvsqtt` | 7 | 3 | 0 | 5 | 1 | under-admission, over-admission, order | fail |
| `ebcnyripm` | 5 | 2 | 0 | 4 | 1 | under-admission, over-admission, order | fail |
| `takohaeosk` | 4 | 2 | 0 | 3 | 1 | under-admission, over-admission, order | fail |
| `hwmvs` | 2 | 2 | - | 0 | 0 | - | pass |
| `qtt` | 27 | 27 | 1 | 0 | 0 | order | fail |
| `ebcn` | 1 | 1 | - | 0 | 0 | - | pass |
| `yripm` | 1 | 1 | - | 0 | 0 | - | pass |
| `tak` | 30 | 17 | 1 | 13 | 0 | under-admission, order | fail |
| `oha` | 11 | 11 | 1 | 0 | 0 | order | fail |
| `eosk` | 1 | 1 | - | 0 | 0 | - | pass |
| `hdaetcu` | 6 | 2 | 0 | 5 | 1 | under-admission, over-admission, order | fail |
| `lyk` | 19 | 19 | 2 | 0 | 0 | order | fail |

The four passing atomic cases do not close CJ-1. The three owner whole-input
rows and both controls remain exact-order failures, and the additional atomic
ordering/admission failures remain visible. Finding CJ-1 and the ignored
`cangjie5_upstream_lane_segmentation_scoring_is_blocked` test stay in force.

## Seven-file closure and replay

The packet contains only the five raw/generated JSON/CSV files, this README,
and `cangjie-manifest.json`. The manifest hashes the other six files and omits
only its own recursive hash. No DLL, compiled table, diagnostic timeout copy,
or other binary payload is copied into evidence.

Replay in a clean disposable checkout at the bound source commit, with the
pinned upstream assets present and all three work/scratch roots absent:

```powershell
if ((git rev-parse HEAD) -ne 'c7c04ff73b76ea3e8e1c5e6bf9b432483ee6650f') {
  throw 'Replay checkout is not at the bound source commit.'
}
if (git status --porcelain) {
  throw 'Replay checkout must start clean.'
}

$scratch = 'target/m59-increment1-cangjie-final'
$oracleWork = 'target/upstream-oracle/1.17.0/m59-cangjie-final-work'
$yuneWork = 'target/m59-increment1-cangjie-yune-work'
foreach ($path in @($scratch, $oracleWork, $yuneWork)) {
  if (Test-Path -LiteralPath $path) {
    throw "Replay path must be absent: $path"
  }
}

cargo build --release -p yune-rime-api

powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File scripts/capture-upstream-cangjie5.ps1 `
  -OracleRoot target/upstream-oracle/1.17.0 `
  -RawOutput "$scratch/cangjie-oracle-raw.json" `
  -Output "$scratch/cangjie-oracle.json" `
  -WorkRoot $oracleWork `
  -KeepWorkRoot `
  -ExpectedRimeDllSha256 86b4c7357d4c6d293ce5589b234d8859ca2ac30923a03bedfa3926eeaf97fb0b `
  -ExpectedRimeDeployerSha256 3abb72b5bb56fcafcfe925d533ae5f832c68d5a0bc9952fd0eea0682fb1ab071 `
  -ExpectedCangjieCommit 52d90a1b1312e74042b38c1cbc8142defbc53171 `
  -ExpectedPreludeCommit 082425ea0684bca36474415d4a0e8db9b016487e `
  -ExpectedLunaPinyinCommit 18a80335c37522311f7cff02886cd81cec3b460a `
  -ExpectedEssayCommit 48c7538f0b760fcc8c9d6bf08711f82cfbd2e9ed `
  -ExpectedStrokeCommit 3a4b0f4013e2b4c14b1e80c92b1d4723eb65f39c `
  -ExpectedCantoneseCommit c99b16e44d2df77a5cb8fb0867dd2bab7a112cb0 `
  -ExpectedLoengfanCommit 987ac95b02f957e8764a2f45222a4006c188ed50

powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File scripts/capture-yune-candidate-order.ps1 `
  -YuneDll target/release/yune_rime_api.dll `
  -SharedDataDir "$oracleWork/shared" `
  -SchemaId cangjie5 `
  -OracleCapture "$scratch/cangjie-oracle.json" `
  -Output "$scratch/cangjie-yune.json" `
  -NarrowSchemaList `
  -WorkRoot $yuneWork `
  -ExpectedYuneDllSha256 2ac5d2550cd462d52d11aec995f609a605ccc6aa9837a8e55cec18a7ec63cfa5 `
  -KeepWorkRoot

python -B scripts/compare-candidate-order.py `
  --oracle "$scratch/cangjie-oracle.json" `
  --actual "$scratch/cangjie-yune.json" `
  --policy exact `
  --output-json "$scratch/cangjie-diff.json" `
  --output-csv "$scratch/cangjie-diff.csv"

if ($LASTEXITCODE -ne 1) {
  throw "Expected the preserved pre-fix exact comparison to exit 1; got $LASTEXITCODE"
}
```

After checking the five expected hashes and the canonical text contract, copy
the five outputs without transformation into a new packet directory.
