# M59 Increment 1 - final Lane B executable pre-fix diagnostic

Generated on 2026-07-10 from clean Yune commit
`94c1c61d3d5b0e77d1c9684118b6e14507b7713f` in the ignored scratch root
`target/m59-increment1-lane-b-final/`, then copied byte-identically into this
six-file packet.

**Verdict: RED, 0/7 accepted.** This is executable pre-fix diagnostic evidence
for the remaining M59/D-48 Lane B exact-order work. It is not acceptance
evidence, does not close D-48 or M59, and applies no exceptions. Comparator
exit `1` is the expected result because all seven strict-exact rows remain
failures.

## Generation and import identity

| Generated and imported file | Bytes | SHA-256 |
|---|---:|---|
| `lane-b-oracle.json` | 714,887 | `8ff369e1a78d0865055003433a258dcf4407136609d4ab8ab5a029c29b989273` |
| `lane-b-yune.json` | 2,781,869 | `2f0513fa28dcc363e7d8b8f53b17b26edf9d8106844065207c28a16239aff8b1` |
| `lane-b-exact-diff.json` | 14,071 | `38a3b5cbbc5ccc72da2041b1a7d5cead6363008fab4004f391b9f53f882baf98` |
| `lane-b-exact-diff.csv` | 4,765 | `5023da9630ffa347cdefe0184992aaa01fda5116d6e69d5bce2cde2ffadce2bb` |

The four packet copies retain the generated bytes exactly. Their embedded
scratch paths were not rewritten. The oracle output records
`existed_before_capture=false` and create-new output; the Yune capture records
the same oracle input hash; comparator provenance binds both scratch inputs,
both outputs, strict `exact` policy, no exception file, effective argv, and the
comparator hash.

All six packet files are canonical UTF-8 without BOM, use LF only, contain no
NUL or CR byte, and end in exactly one LF. Repository attributes resolve these
paths to `text=auto eol=lf`; filtered and no-filter Git blob IDs must remain
identical when staged, proving that Git's LF filter is a no-op.

## Oracle identity and deterministic replay

- Engine: `rime/librime` 1.17.0 at
  `33e78140250125871856cdc5b42ddc6a5fcd3cd4`.
- Schema: upstream `rime/rime-luna-pinyin` at
  `18a80335c37522311f7cff02886cd81cec3b460a`, with clean pinned prelude,
  essay, and stroke dependencies.
- `rime.dll`: SHA-256
  `86b4c7357d4c6d293ce5589b234d8859ca2ac30923a03bedfa3926eeaf97fb0b`.
- `rime_deployer.exe`: SHA-256
  `3abb72b5bb56fcafcfe925d533ae5f832c68d5a0bc9952fd0eea0682fb1ab071`.
- Capture script: `scripts/capture-m59-luna-composition.ps1`, SHA-256
  `c39dc35cfc419bfd8f6ca3c01969609453da6622236e8ee2a103baf32815365f`.
- Curator: `scripts/curate-m59-luna-composition.py` v6, SHA-256
  `09ea46f7b895a9dc51a00466f6de3a1a53640eb8ce9070f83f511f4c7a6e6861`.
- Shared probe: `scripts/oracle-rime-probe.cs`, SHA-256
  `94f7deb7c3632a6c3c918536295b03d88aa8a80bbbbc9d8a26e896fb70bf07e7`.
- Queried shared/build tree hashes are respectively
  `2280114cfc83ada16f3898e6531ff573a7b33a39f1e679695e06eaf8778f3b37`
  and `702c223ffbd282981b9573f881b69f6d97451fe7506ac97cbcecc74c8a4bcc1a`.

The oracle capture uses the ordered inputs `moboyi`, `boyi`, `yi`,
`zhonggao`, `zhongguo`, `gao`, and `guo`. Oracle and Yune use the same ordered
base option policy: `ascii_mode=false`, `full_shape=false`,
`ascii_punct=false`, and `zh_hans=false`. There are no additional option
patches. Every row has page size 5, a present menu, complete all-page capture,
and `termination_reason=last_page`.

The first oracle capture was preserved as ignored
`lane-b-oracle-first.json`. The canonical output path was then regenerated
from a fresh absent path by the same invocation. Both files are 714,887 bytes
and have SHA-256
`8ff369e1a78d0865055003433a258dcf4407136609d4ab8ab5a029c29b989273`.
The first comparator JSON/CSV were likewise preserved as `*-first`; replaying
the comparator to the same canonical output paths reproduced hashes
`38a3b5cbbc5ccc72da2041b1a7d5cead6363008fab4004f391b9f53f882baf98`
and `5023da9630ffa347cdefe0184992aaa01fda5116d6e69d5bce2cde2ffadce2bb`.
This is byte identity, not merely equivalent parsed content.

The fresh oracle was also imported byte-identically as
`crates/yune-core/tests/fixtures/upstream-1.17.0/m59-luna-leading-single-composition.json`.
Against the prior curator-v5 fixture at SHA-256
`5cf3bf5b3b715fa3a7901801e9781e38ee04013ac96355b235a3d4455697b4d3`,
all seven ordered candidate arrays, page arrays, selected-candidate arrays,
input summaries, compositions, and composition snapshots are structurally
identical. Curator v6 adds hardened provenance and explicit
`num_candidates`, `candidate_pointer_null`, `menu_present`, and
`termination_reason` fields; it does not change the captured oracle behavior.

## Yune identity and staging

- Clean source commit:
  `94c1c61d3d5b0e77d1c9684118b6e14507b7713f`, empty status.
- Release DLL: SHA-256
  `2ac5d2550cd462d52d11aec995f609a605ccc6aa9837a8e55cec18a7ec63cfa5`.
- Capture script: `scripts/capture-yune-candidate-order.ps1`, SHA-256
  `f4d91a0d05b3a08e1649a3663e6418878884569878f9e770bbc9a30ba3020c60`.
- Shared probe: SHA-256
  `94f7deb7c3632a6c3c918536295b03d88aa8a80bbbbc9d8a26e896fb70bf07e7`.
- Source shared-data tree: SHA-256
  `c3418eb7d7a677078b0afcb9a958d427a9de0e0a8fc11a41a1707eeb29b865d9`.
- Staged shared-data tree: SHA-256
  `3fa8e927548166b16a9513ae88132c52108eaa88878d14e9fc81075e116cb491`.
- Schema narrowing was explicit through `-NarrowSchemaList`; no tracked
  `default.yaml` overlay was used.

Yune derived the ordered input list from the same fresh oracle. All seven rows
record complete all-page capture and the same present-menu/last-page
termination shape, so the red verdicts are behavior differences rather than
capture truncation.

## Exact verdicts

`First mismatch` is zero-based. No exception file was supplied.

| Input | Oracle | Yune | First mismatch | Missing | Extra | Failure classes | Verdict |
|---|---:|---:|---:|---:|---:|---|---|
| `moboyi` | 225 | 323 | 0 | 1 | 99 | under-admission, over-admission, order | fail |
| `boyi` | 297 | 291 | 0 | 10 | 4 | under-admission, over-admission, order | fail |
| `yi` | 841 | 1,852 | 841 | 0 | 1,011 | over-admission, order | fail |
| `zhonggao` | 117 | 115 | 1 | 2 | 0 | under-admission, order | fail |
| `zhongguo` | 125 | 118 | 0 | 11 | 4 | under-admission, over-admission, order | fail |
| `gao` | 164 | 201 | 133 | 31 | 68 | under-admission, over-admission, order | fail |
| `guo` | 366 | 215 | 155 | 211 | 60 | under-admission, over-admission, order | fail |

## Six-file closure and replay

The packet contains only the four raw/generated JSON/CSV files, this README,
and `lane-b-manifest.json`. The manifest hashes the other five files and omits
only its own recursive hash. No DLL, compiled table, or other binary payload is
copied into evidence.

Replay in a clean disposable checkout at the bound source commit, with the
pinned upstream assets present and the scratch root absent:

```powershell
if ((git rev-parse HEAD) -ne '94c1c61d3d5b0e77d1c9684118b6e14507b7713f') {
  throw 'Replay checkout is not at the bound source commit.'
}
if (git status --porcelain) {
  throw 'Replay checkout must start clean.'
}

$scratch = 'target/m59-increment1-lane-b-final'
if (Test-Path -LiteralPath $scratch) {
  throw "Replay scratch root must be absent: $scratch"
}

cargo build --release -p yune-rime-api

powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File scripts/capture-m59-luna-composition.ps1 `
  -OracleRoot target/upstream-oracle/1.17.0 `
  -Output "$scratch/lane-b-oracle.json" `
  -ExpectedRimeDllSha256 86b4c7357d4c6d293ce5589b234d8859ca2ac30923a03bedfa3926eeaf97fb0b `
  -ExpectedRimeDeployerSha256 3abb72b5bb56fcafcfe925d533ae5f832c68d5a0bc9952fd0eea0682fb1ab071 `
  -ExpectedLunaPinyinCommit 18a80335c37522311f7cff02886cd81cec3b460a `
  -ExpectedPreludeCommit 082425ea0684bca36474415d4a0e8db9b016487e `
  -ExpectedEssayCommit 48c7538f0b760fcc8c9d6bf08711f82cfbd2e9ed `
  -ExpectedStrokeCommit 3a4b0f4013e2b4c14b1e80c92b1d4723eb65f39c

powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File scripts/capture-yune-candidate-order.ps1 `
  -YuneDll target/release/yune_rime_api.dll `
  -SharedDataDir target/upstream-oracle/1.17.0/rime-shared `
  -SchemaId luna_pinyin `
  -OracleCapture "$scratch/lane-b-oracle.json" `
  -Output "$scratch/lane-b-yune.json" `
  -NarrowSchemaList `
  -ExpectedYuneDllSha256 2ac5d2550cd462d52d11aec995f609a605ccc6aa9837a8e55cec18a7ec63cfa5

python -B scripts/compare-candidate-order.py `
  --oracle "$scratch/lane-b-oracle.json" `
  --actual "$scratch/lane-b-yune.json" `
  --policy exact `
  --output-json "$scratch/lane-b-exact-diff.json" `
  --output-csv "$scratch/lane-b-exact-diff.csv"

if ($LASTEXITCODE -ne 1) {
  throw "Expected the preserved pre-fix exact comparison to exit 1; got $LASTEXITCODE"
}
```

After verifying the four expected hashes and canonical text contract, copy the
four outputs without transformation into a new packet directory.
