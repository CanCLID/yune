# Rejected setup attempt 2

This attempt reached the benchmark deploy setup and exited `101` after the C:
drive filled. The source evidence directory contained only the generated
`commands.txt` and `environment.txt`, both preserved here; it contained no
failure log to copy.
