# M61 Phase 0A Requirement And Evidence Review

**Verdict:** APPROVE — no findings.

**Preserved pre-review candidate tree:**
`f8bbe2be5d1525e240dff7977bf3a86c43279927`.

**Base:** commit `7805882d93428db0a3791b0631290ab319b524f0`, tree
`c198d23fc6777ad8b119e30552980243a6acdbb6`.

The plan-only candidate reconstructs exactly from the active M61 plan as its
sole pre-review path. `git diff --check` passes, the real index is empty, local
`main` equals `origin/main`, and ancestry through the kickoff, finalization,
immutable tooling, and both Phase 0 tooling-only repair commits holds.

The preserved Windows preflight is source-bound to the clean base above,
pinned librime `33e78140250125871856cdc5b42ddc6a5fcd3cd4`, official
`rime.dll`, exact shared/build/product trees, and recorded Yune DLL and benchmark
executable hashes. Its strict 170-row comparator reports shape PASS and parity
FAIL at exactly `16/17`: only `zhongdengchangdu` text differs, consistently with
the source-bound detail packet.

The amendment follows D-24 and D-25. It authorizes exactly one structural stale
raw-identity edge correction in `upstream_script_surface_segmentation`, with an
externally sourced pinned-librime fixture and non-circular deployed-Luna test.
It preserves the other sixteen pages, 37/59 Luna guards, reachability, ranking,
selection/recomposition, lifecycle, model checksums, ABI, and Track B. It stops
if the narrow class predicate cannot work.

The tracked comparator, wrapper, and aggregator contract fails closed on less
than `17/17`. Exact `17 + 1` inputs, `9/60/80` cadence, the unchanged M55
registry, frozen M61 `125,000,000 B` supplemental threshold, memory bars,
fixed-binary rules, and retry/measured-red policy remain unchanged. The
correction remains separate from all memory optimization.
